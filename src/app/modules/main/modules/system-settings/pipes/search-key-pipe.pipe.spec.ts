import { SearchKeyPipePipe } from './search-key-pipe.pipe';

describe('SearchKeyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchKeyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
